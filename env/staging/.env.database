# NOTE: staging's DATABASE_URL (.env.api) points at the AWS RDS instance
# above, not at this local container — compose.yml still starts a local
# mysql service unconditionally regardless of ENV, so these values just
# need to exist for that (unused) local container to boot healthy.
MYSQL_ROOT_PASSWORD=secret
MYSQL_DATABASE=referral-tracking
