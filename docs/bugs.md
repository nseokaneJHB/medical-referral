# Known Bugs

Status: **running log of confirmed, still-open defects**, distinct from
`docs/backlog.md` (which is parked/future feature ideas, not defects). Once
a bug here is fixed and verified live, remove its entry — this file tracks
what's still broken, not a fix history. Git history/commits are the record
of what was fixed and how.

_No open bugs currently._
